namespace L
{
    /// <summary>
    /// Empty marker class whose sole purpose is to reference the assembly
    /// that contains the <c>Strings.resx</c> file. The localization API uses
    /// a type to locate resource files, so this class provides that anchor
    /// without adding any behavior.
    /// </summary>
    public class Strings { }
}
